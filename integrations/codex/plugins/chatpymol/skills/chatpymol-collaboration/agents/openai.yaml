interface:
  display_name: "ChatPyMOL 协同编辑"
  short_description: "让 Codex 与浏览器共同维护版本化 PyMOL 场景"
  default_prompt: "使用 $chatpymol-collaboration 打开当前分子工作区，并按我的要求安全修改最新版场景。"
dependencies:
  tools:
    - type: "mcp"
      value: "chatpymol"
      description: "读取并修改 ChatPyMOL Session、结构对象与版本化 PML 场景"
policy:
  allow_implicit_invocation: true
