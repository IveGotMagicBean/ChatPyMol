# 安全策略

## 报告安全问题

请不要用公开 Issue 报告可被利用的漏洞，也不要附带真实 API Key、设备令牌、未公开结构或科研数据。

请发送邮件至 `542058929@qq.com`，并尽量包含：

- 受影响版本或 commit；
- 复现条件和最小复现步骤；
- 可能影响的数据或权限边界；
- 已脱敏的日志、请求或截图；
- 你建议的修复方向（可选）。

维护者会尽力确认和处理，但当前社区项目不承诺固定响应时限或商业 SLA。在修复发布前，请避免公开披露可直接利用的细节。

## 当前安全边界

- 浏览器设备令牌是 bearer 凭据，不是账号或强身份认证；
- 数据默认以单进程文件形式保存在服务器 `DATA_DIR`；
- 百炼/OpenAI 密钥只应存在于服务器环境文件或密钥管理服务；
- 任何获得设备令牌的人都可能访问对应匿名工作区；
- 当前实时事件连接仍通过 URL 查询参数传递设备令牌，可能被反向代理访问日志记录；
- 当前版本没有完整的用户配额、磁盘配额和付费模型额度系统。

因此，当前版本只建议本地使用或部署在有额外访问控制的受控内测环境。向陌生访客开放前，请执行 [生产部署与安全清单](docs/deployment.zh-CN.md)。

## 支持范围

安全修复优先合入 `main`。旧 commit、非官方 fork、修改后的 WASM wheel 和第三方桌面 PyMOL 插件不在本项目的直接支持范围内。
